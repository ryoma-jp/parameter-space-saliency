services:
  pss:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        UID: "${UID}"
        GID: "${GID}"
        USERNAME: "${USERNAME}"
    working_dir: /work
    command: >
      bash -c "jupyter notebook"
    volumes:
      - type: bind
        source: .
        target: /work
      - type: bind
        source: $HOME/work/dataset
        target: /dataset
    ports:
      - 35000:8888
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
